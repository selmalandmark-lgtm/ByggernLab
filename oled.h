
/*#include <stdint.h>

void oled_init();

oled_reset();

oled_home();

oled_go_to_line(line);

oled_clear_line(line);

oled_pos(row, column);

oled_write_data(char); //volatile

oled_print(char*);

oled_set_brightness(lvl);
*/
